# coding: utf-8

"""
    Paystack

    The OpenAPI specification of the Paystack API that merchants and developers can harness to build financial solutions in Africa.

    The version of the OpenAPI document: 1.3.0
    Contact: techsupport@paystack.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from alexasomba_paystack.models.pre_authorization_initialize_metadata import PreAuthorizationInitializeMetadata
from typing import Optional, Set
from typing_extensions import Self

class PreAuthorizationInitialize(BaseModel):
    """
    PreAuthorizationInitialize
    """ # noqa: E501
    amount: StrictStr = Field(description="Amount should be in the subunit of the supported currency")
    email: StrictStr = Field(description="Customer's email address")
    currency: StrictStr = Field(description="Only ZAR is supported for now")
    reference: Optional[StrictStr] = Field(default=None, description="Unique transaction reference. Only -, ., = and alphanumeric characters allowed.")
    callback_url: Optional[StrictStr] = Field(default=None, description="Fully qualified url, e.g. https://example.com/ . Use this to override the callback url provided on the dashboard for this transaction")
    metadata: Optional[PreAuthorizationInitializeMetadata] = None
    split_code: Optional[StrictStr] = Field(default=None, description="The split code of the transaction split. e.g. SPL_98WF13Eb3w")
    subaccount: Optional[StrictStr] = Field(default=None, description="The code for the subaccount that owns the payment. e.g. ACCT_8f4s1eq7ml6rlzj")
    transaction_charge: Optional[StrictInt] = Field(default=None, description="An amount used to override the split configuration for a single split payment. If set, the amount specified goes to the main account regardless of the split configuration.")
    bearer: Optional[StrictStr] = Field(default=None, description="Specifies who will pay the Paystack transaction charges for this transaction. Either account or subaccount (defaults to account).")
    expire_action: Optional[StrictStr] = Field(default=None, description="Specify the action to take on the expiry date. It’s either capture or release. Defaults to release.")
    expire_after_days: Optional[Annotated[int, Field(le=30, strict=True, ge=1)]] = Field(default=None, description="The number of days until the expire_action is executed. The minimum is 1 day and maximum 30 days. Defaults to 5 days.")
    __properties: ClassVar[List[str]] = ["amount", "email", "currency", "reference", "callback_url", "metadata", "split_code", "subaccount", "transaction_charge", "bearer", "expire_action", "expire_after_days"]

    @field_validator('currency')
    def currency_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['ZAR']):
            raise ValueError("must be one of enum values ('ZAR')")
        return value

    @field_validator('bearer')
    def bearer_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['account', 'subaccount']):
            raise ValueError("must be one of enum values ('account', 'subaccount')")
        return value

    @field_validator('expire_action')
    def expire_action_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['capture', 'release']):
            raise ValueError("must be one of enum values ('capture', 'release')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PreAuthorizationInitialize from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of metadata
        if self.metadata:
            _dict['metadata'] = self.metadata.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PreAuthorizationInitialize from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amount": obj.get("amount"),
            "email": obj.get("email"),
            "currency": obj.get("currency"),
            "reference": obj.get("reference"),
            "callback_url": obj.get("callback_url"),
            "metadata": PreAuthorizationInitializeMetadata.from_dict(obj["metadata"]) if obj.get("metadata") is not None else None,
            "split_code": obj.get("split_code"),
            "subaccount": obj.get("subaccount"),
            "transaction_charge": obj.get("transaction_charge"),
            "bearer": obj.get("bearer"),
            "expire_action": obj.get("expire_action"),
            "expire_after_days": obj.get("expire_after_days")
        })
        return _obj


